/* 
Make repository first from the Github website

git init
git status
git add . (dot)
git commit -m "initial commit"
git status
git remote add origin sshLink
git push origin master or, git push origin master --force

*/



// After modification in file
/*
git status
git add --a
git commit -m "initial commit"
git status
git push origin master
*/

// To create master branch
/*
git checkout -b master
*/